name: merchant_app
description: LeBeni's Group - Application Commerçant
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    
  # HTTP & Networking
  dio: ^5.3.3
  
  # Storage sécurisé
  flutter_secure_storage: ^9.0.0
  
  # State Management
  riverpod: ^2.4.9                   # RECOMMANDÉ
  
  # Firebase
  firebase_core: ^2.24.2
  firebase_messaging: ^14.7.9
  
  # Géolocalisation
  geolocator: ^10.1.0
  geocoding: ^2.1.1
  
  # Cartes
  google_maps_flutter: ^2.5.0
  # OU
  flutter_map: ^6.1.0
  
  # Images & Médias
  image_picker: ^1.0.4
  
  # Signature
  signature: ^5.4.1
  
  # Permissions
  permission_handler: ^11.1.0
  
  # Utilitaires
  device_info_plus: ^9.1.0           # Version Android
  intl: ^0.18.1                       # Formatage i18n
  
  # État
  flutter_riverpod: ^2.4.0
  
  # HTTP & API
  dio: ^5.3.3
  
  # Maps & Location
  google_maps_flutter: ^2.5.0
  geolocator: ^10.1.0
  geocoding: ^2.1.1
  
  # Firebase
  firebase_core: ^2.20.0
  firebase_messaging: ^14.7.0
  
  # UI
  cupertino_icons: ^1.0.2
  
  # Navigation
  go_router: ^12.1.0
  
  # Utilities
  intl: ^0.18.1
  image_picker: ^1.0.4
  
  # Local Storage
  shared_preferences: ^2.2.2
  
  # Environment
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
    - .env
