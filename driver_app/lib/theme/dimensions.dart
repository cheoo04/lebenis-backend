// lib/theme/dimensions.dart

/// Dimensions et espacements de l'application
class Dimensions {
  // ========== SPACING ==========

  /// Espacement très petit (4px)
  static const double spacingXS = 4.0;

  /// Espacement petit (8px)
  static const double spacingS = 8.0;

  /// Espacement medium (12px)
  static const double spacingM = 12.0;

  /// Espacement normal (16px)
  static const double spacing = 16.0;

  /// Espacement large (20px)
  static const double spacingL = 20.0;

  /// Espacement très large (24px)
  static const double spacingXL = 24.0;

  /// Espacement extra large (32px)
  static const double spacingXXL = 32.0;

  /// Espacement énorme (48px)
  static const double spacingXXXL = 48.0;

  // ========== PADDING ==========

  /// Padding de la page (16px)
  static const double pagePadding = 16.0;

  /// Padding de la card (12px)
  static const double cardPadding = 12.0;

  /// Padding du bouton horizontal (24px)
  static const double buttonPaddingH = 24.0;

  /// Padding du bouton vertical (12px)
  static const double buttonPaddingV = 12.0;

  /// Padding de l'input horizontal (16px)
  static const double inputPaddingH = 16.0;

  /// Padding de l'input vertical (12px)
  static const double inputPaddingV = 12.0;

  // ========== BORDER RADIUS ==========

  /// Border radius petit (4px)
  static const double radiusS = 4.0;

  /// Border radius medium (8px)
  static const double radiusM = 8.0;

  /// Border radius normal (12px)
  static const double radius = 12.0;

  /// Border radius large (16px)
  static const double radiusL = 16.0;

  /// Border radius très large (24px)
  static const double radiusXL = 24.0;

  /// Border radius circulaire (100px)
  static const double radiusCircle = 100.0;

  // ========== ICON SIZES ==========

  /// Taille d'icône très petite (16px)
  static const double iconXS = 16.0;

  /// Taille d'icône petite (20px)
  static const double iconS = 20.0;

  /// Taille d'icône normale (24px)
  static const double icon = 24.0;

  /// Taille d'icône medium (28px)
  static const double iconM = 28.0;

  /// Taille d'icône large (32px)
  static const double iconL = 32.0;

  /// Taille d'icône très large (40px)
  static const double iconXL = 40.0;

  /// Taille d'icône énorme (48px)
  static const double iconXXL = 48.0;

  // ========== AVATAR SIZES ==========

  /// Avatar très petit (24px)
  static const double avatarXS = 24.0;

  /// Avatar petit (32px)
  static const double avatarS = 32.0;

  /// Avatar normal (40px)
  static const double avatar = 40.0;

  /// Avatar medium (48px)
  static const double avatarM = 48.0;

  /// Avatar large (64px)
  static const double avatarL = 64.0;

  /// Avatar très large (80px)
  static const double avatarXL = 80.0;

  /// Avatar énorme (120px)
  static const double avatarXXL = 120.0;

  // ========== BUTTON SIZES ==========

  /// Hauteur bouton petit (36px)
  static const double buttonHeightS = 36.0;

  /// Hauteur bouton normal (44px)
  static const double buttonHeight = 44.0;

  /// Hauteur bouton medium (48px)
  static const double buttonHeightM = 48.0;

  /// Hauteur bouton large (52px)
  static const double buttonHeightL = 52.0;

  /// Hauteur bouton très large (56px)
  static const double buttonHeightXL = 56.0;

  // ========== INPUT SIZES ==========

  /// Hauteur input (48px)
  static const double inputHeight = 48.0;

  /// Hauteur input petit (40px)
  static const double inputHeightS = 40.0;

  /// Hauteur input large (56px)
  static const double inputHeightL = 56.0;

  // ========== APP BAR ==========

  /// Hauteur de l'AppBar (56px)
  static const double appBarHeight = 56.0;

  /// Hauteur de l'AppBar étendue (120px)
  static const double appBarExtendedHeight = 120.0;

  // ========== BOTTOM NAV BAR ==========

  /// Hauteur de la BottomNavigationBar (60px)
  static const double bottomNavBarHeight = 60.0;

  // ========== CARD ==========

  /// Élévation de la card (2)
  static const double cardElevation = 2.0;

  /// Hauteur minimale de la card (80px)
  static const double cardMinHeight = 80.0;

  // ========== DIVIDER ==========

  /// Épaisseur du divider (1px)
  static const double dividerThickness = 1.0;

  /// Espacement du divider (1px)
  static const double dividerSpace = 1.0;

  // ========== BORDER ==========

  /// Épaisseur de bordure normale (1px)
  static const double borderWidth = 1.0;

  /// Épaisseur de bordure épaisse (2px)
  static const double borderWidthThick = 2.0;

  // ========== SHADOW ==========

  /// Offset de l'ombre
  static const double shadowOffset = 2.0;

  /// Blur de l'ombre
  static const double shadowBlur = 4.0;

  // ========== LIST TILE ==========

  /// Hauteur du ListTile (56px)
  static const double listTileHeight = 56.0;

  /// Padding horizontal du ListTile (16px)
  static const double listTilePaddingH = 16.0;

  /// Padding vertical du ListTile (8px)
  static const double listTilePaddingV = 8.0;

  // ========== DIALOG ==========

  /// Largeur maximale du dialog (400px)
  static const double dialogMaxWidth = 400.0;

  /// Padding du dialog (24px)
  static const double dialogPadding = 24.0;

  // ========== SNACKBAR ==========

  /// Padding du SnackBar (16px)
  static const double snackBarPadding = 16.0;

  /// Margin du SnackBar (8px)
  static const double snackBarMargin = 8.0;

  // ========== FAB ==========

  /// Taille du FloatingActionButton (56px)
  static const double fabSize = 56.0;

  /// Taille du mini FloatingActionButton (40px)
  static const double fabSizeMini = 40.0;

  // ========== CUSTOM ==========

  /// Hauteur du header de section (48px)
  static const double sectionHeaderHeight = 48.0;

  /// Hauteur de la barre de statut (40px)
  static const double statusBarHeight = 40.0;

  /// Largeur du sidebar (280px)
  static const double sidebarWidth = 280.0;

  /// Hauteur de la map (200px)
  static const double mapPreviewHeight = 200.0;

  /// Hauteur de la map complète (400px)
  static const double mapFullHeight = 400.0;
}
